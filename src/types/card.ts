export enum ClassType {
    WARRIOR = 1,
    MAGE,
    NEUTRAL,
    PRIEST,
}

export enum RarityType {
    COMMON = 1,
    RARE,
    EPIC,
    LEGENDARY,
}

export enum RaceType {
    UNDEAD = 1,
    BEAST,
    ELEMENTAL,
    MURLOC,
    DRAENEI,
    PIRATE,
    DRAGON,
    MECH
}

export enum SchoolType {
    FIRE,
    FROST,
    ARCANE,
    HOLY,
    SHADOW,
}