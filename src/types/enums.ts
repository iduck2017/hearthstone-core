export enum CardType {
    MINION = 1,
    SPELL,
    WEAPON,
    HERO
}

export enum RaceType {
    UNDEAD = 1,
    BEAST,
    ELEMENTAL,
    MURLOC,
}

export enum QueryMode {
    INCLUDE = 1,
    EXCLUDE,
}
